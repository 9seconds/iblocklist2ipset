language: python

python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "pypy"

install:
  - "pip install --use-mirrors -r requirements.txt"

script:
  - py.test tests
  - prospector --strictness veryhigh -F -B -o grouped

notifications:
  email:
    - nineseconds@yandex.ru

matrix:
  allow_failures:
    - python: "3.4"